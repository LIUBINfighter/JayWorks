---
title: 插件设置参考（占位）
description: 预留用于说明 JayWorks 插件的可配置项、默认值及其作用。
draft: true
updated: 2025-09-28
---

此页面尚未编写，将在插件暴露正式可配置项后补充：

拟包含内容：

| 设置键 | 默认值 | 说明 | 备注 |
| ------ | ------ | ---- | ---- |
| （示例）enableMermaid | true | 是否启用 Mermaid 渲染 | 关闭后所有 ```mermaid 代码块保持原样 |
| （示例）maxCodeBlockLines | 400 | 超出时折叠代码块 | 交互式展开 | 

如果你正在贡献配置功能，请：
1. 在代码中增加类型定义（settings.ts / interface）。
2. 在保存逻辑中保证向后兼容（提供默认值填充）。
3. 更新本页并移除 `draft: true`。
